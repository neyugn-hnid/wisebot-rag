server:
  port: 9000
  compression:
    enabled: true
    mime-types: application/json

spring:
  application:
    name: api-gateway
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    root: INFO
    web: INFO